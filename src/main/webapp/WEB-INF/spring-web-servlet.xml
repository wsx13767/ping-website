<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:task="http://www.springframework.org/schema/task"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
	<!-- 自動掃描包名 Spring MVC @Controller -->
	<context:component-scan base-package="com.ping" />

	<!-- 自動註冊DefaultAnnotationHandlerMapping與AnnotationMethodHandlerAdapter -->
	<mvc:annotation-driven />

	<!-- Default View Resolver -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/views/" />
		<property name="suffix" value=".jsp" />
	</bean>
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/*"/>
			<bean class="com.ping.inter.LoginInterceptor"></bean>
		</mvc:interceptor>
		<mvc:interceptor>
			<mvc:mapping path="/com/ping/controller/RegisterLogin.java"/>
			<bean class="com.ping.inter.LoginInAlreadyterceptor"></bean>
		</mvc:interceptor>
	</mvc:interceptors>
	<!-- spring maping 靜態資源路徑 -->
	<mvc:resources location="/resources/" mapping="/resources/**" />

	<import resource="/dbSourceConfig.xml"/>
</beans>